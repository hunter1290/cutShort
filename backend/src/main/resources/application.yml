spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/urlshortner}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:local-dev-secret-replace-in-prod-must-be-at-least-256-bits-long-abc123}
    expiration-ms: 86400000   # 24 h
  base-url: ${BASE_URL:http://localhost:8080}
  frontend-url: ${FRONTEND_URL:http://localhost:3000}
  anonymous-expiry-days: 7   # URLs created without auth expire after 7 days
